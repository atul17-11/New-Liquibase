pipeline {
  
  agent any
  
  stages {
    stage('Status') {
      steps {
        sh 'liquibase status --url="jdbc:sqlserver://liquibase-demoserver.database.windows.net:1433;database=Liquibase-Demo-db;user=adminlogin@liquibase-demoserver;password={your_password_here};encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30"'
      }
    }
    stage('Update') {
      steps {
        sh 'liquibase update --url="jdbc:sqlserver://liquibase-demoserver.database.windows.net:1433;database=Liquibase-Demo-db;user=adminlogin@liquibase-demoserver;password={your_password_here};encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30"'
      }
    }
  }
  post {
    always {
      cleanWs()
    }
  }
}
